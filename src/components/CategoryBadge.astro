---
interface Props {
  category: "battle" | "leader" | "strategy" | "books";
}
const { category } = Astro.props;

const map = {
  battle:   { label: "合戦分析",      icon: "⚔",  href: "/category/battle/" },
  leader:   { label: "武将の思想",    icon: "🏯", href: "/category/leader/" },
  strategy: { label: "兵法・現代応用", icon: "📜", href: "/category/strategy/" },
  books:    { label: "書籍レビュー",   icon: "📚", href: "/category/books/" },
};
const { label, icon, href } = map[category];
---

<a href={href} class="category-badge" data-cat={category}>
  <span class="badge-icon" aria-hidden="true">{icon}</span>
  <span class="badge-label">{label}</span>
</a>

<style>
  .category-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    font-family: var(--font-sans);
    font-size: 0.68rem;
    font-weight: 600;
    letter-spacing: 0.12em;
    padding: 0.22em 0.7em;
    border-radius: 2px;
    color: var(--orange);
    background: rgba(232, 85, 45, 0.08);
    border: 1px solid rgba(232, 85, 45, 0.25);
    text-transform: uppercase;
    text-decoration: none;
    transition: background 0.2s, border-color 0.2s;
    white-space: nowrap;
  }
  .category-badge:hover {
    background: rgba(232, 85, 45, 0.15);
    border-color: rgba(232, 85, 45, 0.5);
  }
  .badge-icon { font-size: 0.78rem; }
</style>
