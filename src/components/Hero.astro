---
---

<section class="hero" aria-label="ヒーロー">
  <div class="hero-inner container">

    <!-- 左：テキスト＋CTA -->
    <div class="hero-content">
      <p class="hero-eyebrow">
        <span class="eyebrow-line"></span>
        SENGOKU × MODERN STRATEGY
      </p>

      <h1 class="hero-title">
        戦国武将の判断が、<br>
        <em>あなたの正解</em>になる。
      </h1>

      <p class="hero-body">
        命がかかった合戦室で磨かれた戦略は、<br class="br-pc">
        現代の会議・交渉・組織にそのまま使える。<br class="br-pc">
        500年前の知恵を、今日の武器に変える。
      </p>

      <div class="hero-cta">
        <a href="/blog/" class="cta-primary">記事を読む →</a>
        <a href="/books/" class="cta-ghost">おすすめ書籍</a>
      </div>
    </div>

    <!-- 右：ダーク藍パネル＋スタッツ -->
    <div class="hero-panel" aria-hidden="true">
      <span class="panel-deco">戦</span>
      <div class="panel-stats">
        <div class="stat">
          <span class="stat-num font-italic">01</span>
          <span class="stat-label">合戦×ビジネス</span>
        </div>
        <div class="stat">
          <span class="stat-num font-italic">02</span>
          <span class="stat-label">武将×リーダーシップ</span>
        </div>
        <div class="stat">
          <span class="stat-num font-italic">03</span>
          <span class="stat-label">兵法×現代戦略</span>
        </div>
        <div class="stat">
          <span class="stat-num font-italic">04</span>
          <span class="stat-label">書籍レビュー</span>
        </div>
      </div>
    </div>

  </div>
</section>

<style>
  .hero {
    background: var(--bg);
    min-height: 90vh;
    display: flex;
    align-items: center;
  }

  .hero-inner {
    display: grid;
    grid-template-columns: 1fr 360px;
    gap: 4rem;
    align-items: center;
    padding-top: calc(var(--nav-height) + 4rem);
    padding-bottom: 5rem;
  }

  /* ── 左：コンテンツ ── */
  .hero-content {
    display: flex;
    flex-direction: column;
    gap: 2.4rem;
    max-width: 620px;
  }

  .hero-eyebrow {
    display: flex;
    align-items: center;
    gap: 1rem;
    font-family: var(--font-sans);
    font-size: 0.7rem;
    font-weight: 500;
    letter-spacing: 0.3em;
    color: var(--gold);
    text-transform: uppercase;
  }
  .eyebrow-line {
    display: inline-block;
    width: 2.5rem;
    height: 1px;
    background: var(--gold);
    flex-shrink: 0;
  }

  .hero-title {
    font-family: var(--font-serif);
    font-size: clamp(2.4rem, 5vw, 4.2rem);
    font-weight: 700;
    color: var(--ink);
    line-height: 1.25;
    letter-spacing: 0.02em;
  }
  .hero-title em {
    font-style: normal;
    color: var(--gold);
    border-bottom: 2px solid var(--gold-light);
  }

  .hero-body {
    font-family: var(--font-serif);
    font-size: clamp(0.9rem, 1.4vw, 1rem);
    font-weight: 400;
    line-height: 2.1;
    color: var(--ink2);
    border-left: 2px solid var(--gold-light);
    padding-left: 1.2rem;
  }

  .hero-cta {
    display: flex;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .cta-primary {
    display: inline-flex;
    align-items: center;
    padding: 0.9rem 2.4rem;
    background: var(--ink);
    color: #fff;
    font-family: var(--font-sans);
    font-size: 0.88rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    border-radius: 2px;
    text-decoration: none;
    transition: background 0.2s, transform 0.2s;
  }
  .cta-primary:hover {
    background: var(--accent);
    transform: translateY(-1px);
  }

  .cta-ghost {
    display: inline-flex;
    align-items: center;
    padding: 0.9rem 2rem;
    background: transparent;
    color: var(--ink2);
    font-family: var(--font-sans);
    font-size: 0.85rem;
    font-weight: 400;
    letter-spacing: 0.05em;
    border: 1px solid var(--rule);
    border-radius: 2px;
    text-decoration: none;
    transition: border-color 0.2s, color 0.2s;
  }
  .cta-ghost:hover {
    border-color: var(--gold);
    color: var(--gold);
  }

  /* ── 右：ダーク藍パネル ── */
  .hero-panel {
    background: var(--accent);
    border-radius: 4px;
    padding: 3rem 2.4rem;
    position: relative;
    overflow: hidden;
    height: 420px;
    display: flex;
    align-items: flex-end;
  }

  .panel-deco {
    position: absolute;
    top: -1rem;
    right: -1.5rem;
    font-family: var(--font-serif);
    font-size: 14rem;
    font-weight: 800;
    color: rgba(255, 255, 255, 0.05);
    line-height: 1;
    user-select: none;
    pointer-events: none;
  }

  .panel-stats {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.8rem 1.5rem;
    width: 100%;
    position: relative;
    z-index: 1;
  }

  .stat {
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
  }

  .stat-num {
    font-family: var(--font-italic);
    font-size: 2rem;
    font-weight: 400;
    font-style: italic;
    color: var(--gold);
    line-height: 1;
  }

  .stat-label {
    font-family: var(--font-sans);
    font-size: 0.72rem;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.55);
    letter-spacing: 0.06em;
  }

  /* ── レスポンシブ ── */
  @media (max-width: 900px) {
    .hero-inner {
      grid-template-columns: 1fr;
      padding-top: calc(var(--nav-height) + 3rem);
      padding-bottom: 4rem;
    }
    .hero-panel {
      height: auto;
      padding: 2rem;
    }
    .panel-deco { font-size: 8rem; }
  }

  @media (max-width: 480px) {
    .hero-inner {
      padding-top: calc(var(--nav-height) + 2rem);
      padding-bottom: 3rem;
      gap: 2.5rem;
    }
    .hero-panel { display: none; }
    .br-pc { display: none; }
  }
</style>
