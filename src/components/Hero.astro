---
---

<section class="hero" aria-label="ヒーロー">
  <div class="hero-left">
    <p class="hero-eyebrow">History × Modern Strategy</p>

    <h1 class="hero-title">
      歴史の戦略を、<br />現代の武器に。
    </h1>

    <p class="hero-lead">
      関ヶ原の調略、孫子の情報戦、信長の破壊的イノベーション——<br />
      数百年前の知恵は、現代の交渉・組織・意思決定に驚くほど直結している。
    </p>

    <div class="hero-cta">
      <a href="/blog/" class="hero-btn-primary">記事を読む →</a>
      <a href="/books/" class="hero-btn-secondary">おすすめ書籍</a>
    </div>

    <div class="hero-stats">
      <div class="hero-stat">
        <span class="stat-num">2</span>
        <span class="stat-label">記事公開中</span>
      </div>
      <div class="hero-stat">
        <span class="stat-num">4</span>
        <span class="stat-label">カテゴリ</span>
      </div>
      <div class="hero-stat">
        <span class="stat-num">450+</span>
        <span class="stat-label">年の戦略史</span>
      </div>
    </div>
  </div>

  <div class="hero-right" aria-hidden="true">
    <div class="hero-feature-card">
      <p class="feature-label">注目カテゴリ</p>
      <ul class="feature-list">
        <li><span class="feat-icon">⚔</span> 合戦と戦略 — 関ヶ原・川中島を読み解く</li>
        <li><span class="feat-icon">🏯</span> 武将の思想 — 信長・家康の決断力</li>
        <li><span class="feat-icon">📜</span> 兵法現代応用 — 孫子をビジネスへ</li>
        <li><span class="feat-icon">📚</span> 書籍レビュー — 厳選アフィリエイト</li>
      </ul>
    </div>
    <div class="hero-deco" aria-hidden="true">戦</div>
  </div>
</section>

<style>
  .hero {
    min-height: calc(100vh - var(--nav-height));
    display: grid;
    grid-template-columns: 1fr 1fr;
    overflow: hidden;
    padding-top: var(--nav-height);
  }

  /* 左カラム */
  .hero-left {
    padding: 5rem 4rem 4rem 6rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 2rem;
    background: var(--bg);
  }

  .hero-eyebrow {
    font-family: var(--font-sans);
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.3em;
    color: var(--orange);
    text-transform: uppercase;
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }
  .hero-eyebrow::before {
    content: "";
    display: inline-block;
    width: 2.5rem;
    height: 2px;
    background: var(--orange);
  }

  .hero-title {
    font-family: var(--font-serif);
    font-size: clamp(2.6rem, 4.5vw, 4rem);
    font-weight: 800;
    color: var(--text);
    letter-spacing: 0.03em;
    line-height: 1.25;
  }

  .hero-lead {
    font-family: var(--font-serif);
    font-size: 0.95rem;
    font-weight: 400;
    line-height: 2;
    color: var(--text-mid);
    max-width: 420px;
  }

  .hero-cta {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .hero-btn-primary {
    display: inline-block;
    padding: 0.9rem 2.2rem;
    background: var(--orange);
    color: #fff;
    font-family: var(--font-sans);
    font-size: 0.9rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    border-radius: 3px;
    transition: background 0.2s, transform 0.2s;
    text-decoration: none;
  }
  .hero-btn-primary:hover {
    background: var(--orange-dark);
    transform: translateY(-1px);
  }

  .hero-btn-secondary {
    display: inline-block;
    padding: 0.9rem 2rem;
    background: transparent;
    color: var(--text);
    font-family: var(--font-sans);
    font-size: 0.9rem;
    font-weight: 500;
    letter-spacing: 0.05em;
    border: 2px solid var(--border-mid);
    border-radius: 3px;
    transition: border-color 0.2s, color 0.2s;
    text-decoration: none;
  }
  .hero-btn-secondary:hover {
    border-color: var(--orange);
    color: var(--orange);
  }

  /* スタッツ */
  .hero-stats {
    display: flex;
    gap: 2.5rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--border);
  }
  .hero-stat {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
  }
  .stat-num {
    font-family: var(--font-serif);
    font-size: 1.8rem;
    font-weight: 800;
    color: var(--orange);
    line-height: 1;
  }
  .stat-label {
    font-family: var(--font-sans);
    font-size: 0.7rem;
    font-weight: 500;
    color: var(--text-sub);
    letter-spacing: 0.05em;
  }

  /* 右カラム */
  .hero-right {
    background: var(--bg-navy);
    position: relative;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 4rem 3rem;
  }

  .hero-deco {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-family: var(--font-serif);
    font-size: 28vw;
    font-weight: 800;
    color: rgba(232, 85, 45, 0.07);
    line-height: 1;
    pointer-events: none;
    user-select: none;
  }

  .hero-feature-card {
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 6px;
    padding: 2.4rem;
    position: relative;
    z-index: 1;
    width: 100%;
    max-width: 380px;
  }

  .feature-label {
    font-family: var(--font-sans);
    font-size: 0.68rem;
    font-weight: 600;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--orange);
    margin-bottom: 1.6rem;
  }

  .feature-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 1.2rem;
  }

  .feature-list li {
    font-family: var(--font-sans);
    font-size: 0.88rem;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.82);
    line-height: 1.6;
    display: flex;
    align-items: flex-start;
    gap: 0.6rem;
  }

  .feat-icon {
    flex-shrink: 0;
    font-size: 1rem;
    margin-top: 0.05em;
  }

  /* レスポンシブ */
  @media (max-width: 960px) {
    .hero {
      grid-template-columns: 1fr;
      min-height: auto;
    }
    .hero-left {
      padding: 4rem 2rem 3rem;
    }
    .hero-right {
      min-height: 40vh;
      padding: 3rem 2rem;
    }
    .hero-deco { font-size: 50vw; }
  }

  @media (max-width: 480px) {
    .hero-left {
      padding: 3rem 1.2rem 2.5rem;
      gap: 1.5rem;
    }
    .hero-stats { gap: 1.5rem; }
  }
</style>
