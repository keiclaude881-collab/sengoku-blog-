---
const year = new Date().getFullYear();
const cols = [
  {
    title: "カテゴリ",
    links: [
      { label: "合戦と戦略", href: "/category/battle/" },
      { label: "武将の思想", href: "/category/leader/" },
      { label: "兵法・現代応用", href: "/category/strategy/" },
      { label: "書籍レビュー", href: "/category/books/" },
    ],
  },
  {
    title: "サイト情報",
    links: [
      { label: "記事一覧", href: "/blog/" },
      { label: "おすすめ書籍", href: "/books/" },
      { label: "このブログについて", href: "/about/" },
      { label: "プライバシーポリシー", href: "/privacy/" },
    ],
  },
];
---

<footer class="site-footer">
  <div class="footer-inner container">
    <div class="footer-brand">
      <a href="/" class="footer-logo">
        戦略の<span>系譜</span>
      </a>
      <p class="footer-tagline">歴史の戦略を、現代の武器に。</p>
      <p class="footer-desc">
        戦国・兵法の知恵を現代のビジネス・交渉・リーダーシップに応用する、日本史×戦略思考ブログ。
      </p>
    </div>

    <nav class="footer-nav" aria-label="フッターナビ">
      {cols.map(col => (
        <div class="footer-col">
          <h3 class="footer-col-title">{col.title}</h3>
          <ul>
            {col.links.map(({ label, href }) => (
              <li><a href={href}>{label}</a></li>
            ))}
          </ul>
        </div>
      ))}
    </nav>
  </div>

  <div class="footer-bottom">
    <div class="container footer-bottom-inner">
      <p class="copy">&copy; {year} 戦略の系譜. All rights reserved.</p>
      <p class="affiliate-notice">
        本サイトはAmazonアソシエイトプログラムに参加しています。
      </p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    background: var(--bg-dark);
    color: var(--bg);
    position: relative;
    z-index: 1;
    margin-top: auto;
  }

  .footer-inner {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: var(--sp-xl);
    padding-top: var(--sp-xl);
    padding-bottom: var(--sp-lg);
  }

  .footer-logo {
    display: inline-block;
    font-family: var(--font-serif);
    font-size: 1.2rem;
    font-weight: 800;
    letter-spacing: 0.15em;
    color: var(--bg);
    margin-bottom: 0.8rem;
    text-decoration: none;
  }
  .footer-logo span { color: var(--red); }

  .footer-tagline {
    font-family: var(--font-serif);
    font-size: 0.88rem;
    color: var(--red);
    letter-spacing: 0.08em;
    margin-bottom: 0.8rem;
  }

  .footer-desc {
    font-family: var(--font-sans);
    font-size: 0.8rem;
    font-weight: 400;
    line-height: 1.8;
    color: rgba(255, 255, 255, 0.55);
    max-width: 360px;
  }

  .footer-nav {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--sp-lg);
  }

  .footer-col-title {
    font-family: var(--font-sans);
    font-size: 0.68rem;
    font-weight: 400;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: var(--red);
    margin-bottom: 1rem;
  }

  .footer-col ul {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }

  .footer-col a {
    font-family: var(--font-sans);
    font-size: 0.8rem;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.6);
    text-decoration: none;
    transition: color 0.3s;
    letter-spacing: 0.05em;
  }
  .footer-col a:hover { color: var(--bg); }

  .footer-bottom {
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    padding: 1.2rem 0;
  }

  .footer-bottom-inner {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .copy, .affiliate-notice {
    font-family: var(--font-sans);
    font-size: 0.72rem;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.35);
    letter-spacing: 0.05em;
  }

  @media (max-width: 768px) {
    .footer-inner {
      grid-template-columns: 1fr;
      gap: var(--sp-lg);
    }
    .footer-bottom-inner {
      flex-direction: column;
      align-items: flex-start;
    }
  }
</style>
