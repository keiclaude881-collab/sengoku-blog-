---
interface Props {
  lesson: string;
}
const { lesson } = Astro.props;
---

<div class="lesson-box">
  <span class="lesson-prefix">💡 現代への教訓：</span>{lesson}
</div>

<style>
  .lesson-box {
    padding: 0.8rem 1rem;
    background: rgba(192, 57, 43, 0.05);
    border-left: 2px solid var(--red);
    font-size: 0.78rem;
    line-height: 1.8;
    font-style: italic;
    color: var(--text-mid);
    margin: 1.2rem 0;
  }
  .lesson-prefix {
    font-style: normal;
    font-weight: 600;
    color: var(--red);
    margin-right: 0.3em;
  }
</style>
