---
import { getCollection } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
import Hero from "../components/Hero.astro";
import ArticleFeatured from "../components/ArticleFeatured.astro";
import ArticleCard from "../components/ArticleCard.astro";
import BookCard from "../components/BookCard.astro";
import AffiliateDisclosure from "../components/AffiliateDisclosure.astro";

const formspreeId = import.meta.env.FORMSPREE_ID ?? '';

const allArticles = (await Promise.all([
  getCollection("battle"),
  getCollection("leader"),
  getCollection("strategy").catch(() => []),
  getCollection("books").catch(() => []),
  getCollection("movie").catch(() => []),
  getCollection("whatif").catch(() => []),
  getCollection("anime").catch(() => []),
  getCollection("business").catch(() => []),
  getCollection("sports").catch(() => []),
])).flat().sort((a, b) => b.data.date.getTime() - a.data.date.getTime());

const featured = allArticles.find(a => a.data.featured) ?? allArticles[0];
const recent   = allArticles.filter(a => a.id !== featured?.id).slice(0, 3);

function slug(col: string, id: string) {
  return `${col}/${id.replace(/\.mdx?$/, '')}`;
}

const featuredBooks = [
  { title: "孫子の兵法（知的生きかた文庫）", asin: "4837900186", reason: "情報戦の原典。競合分析の基礎が凝縮されている" },
  { title: "五輪書（宮本武蔵）岩波文庫", asin: "4003300211", reason: "個の強さとは何か。武蔵の戦略哲学を学ぶ" },
  { title: "戦略の本質", asin: "4532194628", reason: "日本の合戦を経営戦略の視点で再解釈した名著" },
  { title: "失敗の本質", asin: "4122018331", reason: "なぜ日本軍は負けたのか。組織論の古典" },
];

const marqueeItems = [
  "関ヶ原の決断",
  "孫子の兵法",
  "信長のイノベーション",
  "家康の忍耐戦略",
  "桶狭間の奇襲",
  "武田信玄の人事",
  "五輪書の哲学",
  "ナポレオンの速戦",
  "戦略的情報収集",
  "謀攻篇の現代応用",
];
---

<BaseLayout
  title="決断の系譜 | 迷ったとき、偉人の知恵がそっと寄り添う。"
  description="仕事・育児・人間関係に疲れたとき、歴史の武将・偉人・物語の主人公たちの決断が、今日を生きるヒントをくれる。"
>
  <!-- ヒーロー -->
  <Hero />

  <!-- マーキー帯 -->
  <div class="marquee-band" aria-hidden="true">
    <div class="marquee-track">
      {[...marqueeItems, ...marqueeItems].map(item => (
        <span class="marquee-item">
          <span class="marquee-dot">✦</span>
          {item}
        </span>
      ))}
    </div>
  </div>

  <!-- イントロバンド -->
  <section class="intro-band">
    <div class="container intro-inner">
      <span class="intro-label" aria-hidden="true">Philosophy</span>
      <blockquote class="intro-quote">
        <p class="quote-jp">「戦わずして人の兵を屈するは、善の善なる者なり。」</p>
        <p class="quote-en font-italic">— Sun Tzu, The Art of War</p>
      </blockquote>
    </div>
  </section>

  <!-- メルマガ登録 -->
  <section class="email-section">
    <div class="container email-inner">
      <div class="email-left">
        <h2 class="email-heading">疲れた週末に読みたい、一通のメール。</h2>
        <ul class="email-benefits">
          <li><span class="benefit-check">✓</span> 週1回、偉人の決断と知恵をあなたのもとへ</li>
          <li><span class="benefit-check">✓</span> 「今週も頑張れた」と思えるひとことコラム</li>
          <li><span class="benefit-check">✓</span> 新着記事の先行通知</li>
        </ul>
        <p class="email-note">無料・いつでも解除可能</p>
      </div>
      <div class="email-right">
        <form
          class="email-form"
          id="newsletter-form"
          action="https://ketsudankeifu.substack.com/subscribe"
          method="get"
        >
          <label class="form-label" for="email-input">メールアドレス</label>
          <div class="form-row">
            <input
              type="email"
              id="email-input"
              name="email"
              placeholder="your@email.com"
              required
              class="form-input"
            />
            <button type="submit" class="form-btn">登録する →</button>
          </div>
          <p class="form-privacy">
            <a href="/privacy/">プライバシーポリシー</a>に同意のうえご登録ください。
          </p>
        </form>
        <div class="email-thanks" id="email-thanks" hidden>
          <p class="thanks-icon">✓</p>
          <p class="thanks-text">ご登録ありがとうございます。<br>確認メールをお送りします。</p>
        </div>
      </div>
    </div>
  </section>

  <!-- 注目記事 -->
  {allArticles.length > 0 && (
    <section class="section articles-section">
      <div class="container">
        <div class="section-header">
          <h2 class="section-title">注目記事</h2>
          <a href="/blog/" class="section-more">すべて見る →</a>
        </div>

        {featured && (
          <ArticleFeatured
            title={featured.data.title}
            description={featured.data.description}
            date={featured.data.date}
            category={featured.data.category}
            tags={featured.data.tags}
            readingTime={featured.data.readingTime}
            lesson={featured.data.lesson}
            slug={slug(featured.collection, featured.id)}
          />
        )}

        {recent.length > 0 && (
          <ul class="article-grid" role="list">
            {recent.map(post => (
              <li>
                <ArticleCard
                  title={post.data.title}
                  description={post.data.description}
                  date={post.data.date}
                  category={post.data.category}
                  tags={post.data.tags}
                  readingTime={post.data.readingTime}
                  lesson={post.data.lesson}
                  slug={slug(post.collection, post.id)}
                  ogImage={post.data.ogImage}
                />
              </li>
            ))}
          </ul>
        )}
      </div>
    </section>
  )}

  <!-- カテゴリ -->
  <section class="categories-section section">
    <div class="container">
      <div class="section-header">
        <h2 class="section-title">あなたに寄り添う、偉人たちの視点。</h2>
      </div>
      <ul class="cat-grid" role="list">
        {[
          {
            cat: "battle",
            label: "合戦と戦略",
            hook: "あの戦いも、誰かの迷いから始まった",
            desc: "追い詰められた武将たちの決断は、今日の「どうすればいい？」に答えてくれる",
          },
          {
            cat: "leader",
            label: "武将の思想",
            hook: "信長も家康も、最初はただの人間だった",
            desc: "完璧じゃない彼らが、それでも前へ進めた理由",
          },
          {
            cat: "strategy",
            label: "兵法・知恵",
            hook: "孫子の言葉は、疲れた心にも効く",
            desc: "2500年前の知恵が、今日の判断をそっと助けてくれる",
          },
          {
            cat: "books",
            label: "書籍レビュー",
            hook: "忙しい毎日の中で、この一冊があなたを変える",
            desc: "読み終わった後、世界の見え方が少し変わる本を紹介",
          },
          {
            cat: "movie",
            label: "映画の中の知恵",
            hook: "スクリーンの向こうに、あなたへのメッセージがある",
            desc: "名作映画が描く人間の決断と葛藤を、一緒に読み解く",
          },
          {
            cat: "whatif",
            label: "歴史IF",
            hook: "もし違う選択をしていたら、どうなっていたか",
            desc: "「あの時こうしていれば」——歴史のIFが教える選択の重さ",
          },
          {
            cat: "anime",
            label: "アニメの中の知恵",
            hook: "好きなアニメのあのシーン、実は深い意味があった",
            desc: "キャラクターたちの葛藤と決断が、今日の自分に重なる",
          },
          {
            cat: "business",
            label: "ビジネスの知恵",
            hook: "仕事で迷ったとき、偉人ならどうするか",
            desc: "歴史の知恵を借りて、今日の仕事をすこし楽にする",
          },
          {
            cat: "sports",
            label: "勝負師の知恵",
            hook: "土壇場で諦めなかった人たちの共通点",
            desc: "スポーツの名勝負が教える、粘り方と潔い撤退の作法",
          },
        ].map(({ cat, label, hook, desc }) => (
          <li>
            <a href={`/category/${cat}/`} class="cat-card">
              <span class="cat-name">{label}</span>
              <h3 class="cat-hook">{hook}</h3>
              <p class="cat-desc">{desc}</p>
              <span class="cat-link">記事一覧 →</span>
            </a>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- 映画コーナーバナー -->
  <section class="cinema-banner">
    <div class="container cinema-inner">
      <div class="cinema-text">
        <span class="cinema-label">CINEMA &amp; STRATEGY</span>
        <h2 class="cinema-heading">映画で学ぶ、戦略の本質。</h2>
        <p class="cinema-body">七人の侍からスター・ウォーズまで。U-NEXT・Hulu・Netflix・Disney+で観られる作品を戦略の視点で読み解く特設コーナー。</p>
        <div class="cinema-services" aria-hidden="true">
          <span class="cs-badge" style="background:#0045ff">U</span>
          <span class="cs-badge" style="background:#3dbb3d">H</span>
          <span class="cs-badge" style="background:#e50914">N</span>
          <span class="cs-badge" style="background:#113ccf">D+</span>
        </div>
      </div>
      <a href="/movies/" class="cinema-cta">映画コーナーへ →</a>
    </div>
  </section>

  <!-- 悩み別おすすめ -->
  <section class="needs-section section">
    <div class="container">
      <div class="section-header">
        <h2 class="section-title">今、どんな気持ちで来ましたか？</h2>
        <p class="needs-sub">気持ちから記事を探す</p>
      </div>
      <ul class="needs-grid" role="list">
        {[
          {
            tag: "意思決定",
            label: "決められなくて、疲れた",
            desc: "正解なんてなかった——それでも決断し続けた人たちの話",
          },
          {
            tag: "リーダーシップ",
            label: "誰かを支えるのが、しんどい",
            desc: "信長も家康も孤独だった。それでも人を率いた理由",
          },
          {
            tag: "ビジネス戦略",
            label: "仕事がうまくいかない",
            desc: "劣勢でも勝った人たちが使っていた、シンプルな考え方",
          },
          {
            tag: "組織論",
            label: "もっと自分らしく生きたい",
            desc: "既存のルールを壊して生きた偉人たちの、それぞれの道",
          },
        ].map(({ tag, label, desc }) => (
          <li>
            <a href={`/blog/?tag=${encodeURIComponent(tag)}`} class="needs-card" data-tag={tag}>
              <span class="needs-tag">#{tag}</span>
              <h3 class="needs-label">{label}</h3>
              <p class="needs-desc">{desc}</p>
              <span class="needs-cta">記事を読む →</span>
            </a>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- 書籍セクション -->
  <section class="books-section" aria-label="おすすめ書籍">
    <div class="container books-inner">
      <div class="section-header books-header">
        <h2 class="section-title books-title">戦略を深める、厳選の一冊。</h2>
        <a href="/books/" class="books-more">すべて見る →</a>
      </div>
      <p class="books-lead">記事を読んで「もっと深く知りたい」と思ったら、この本から始めよう。</p>
      <AffiliateDisclosure />
      <div class="book-grid">
        {featuredBooks.map(b => (
          <BookCard title={b.title} asin={b.asin} reason={b.reason} />
        ))}
      </div>
    </div>
  </section>

  <!-- Audibleバナー -->
  <section class="audible-banner">
    <div class="container banner-inner">
      <div class="banner-text">
        <span class="banner-tag audible-tag">Audible</span>
        <h3 class="banner-heading audible-heading">通勤中に戦略書を聴く。</h3>
        <p class="banner-body audible-body">孫子・五輪書・ビジネス書——移動時間が「戦略の学習時間」に変わる。30日間無料で体験できる。</p>
      </div>
      <a
        href="https://www.amazon.co.jp/audible?tag=sengokublog20-22"
        target="_blank"
        rel="nofollow sponsored noopener"
        class="banner-cta audible-cta"
      >
        30日間 無料体験 →
      </a>
    </div>
  </section>

  <!-- noteバナー -->
  <section class="note-banner">
    <div class="container banner-inner">
      <div class="banner-text">
        <span class="banner-tag note-tag">note</span>
        <h3 class="banner-heading note-heading">もっと深く知りたい人へ。</h3>
        <p class="banner-body note-body">ブログの一歩先へ。じっくり読みたい夜のための、深掘りコンテンツをnoteで。</p>
      </div>
      <a
        href="https://note.com/ketsudan_keifu"
        target="_blank"
        rel="noopener"
        class="banner-cta note-cta"
      >
        noteで購入する →
      </a>
    </div>
  </section>

</BaseLayout>

<!-- スティッキーCTAバー -->
<div class="sticky-cta" id="sticky-cta" aria-label="常設CTA">
  <div class="sticky-inner">
    <div class="sticky-items">
      <a href="#newsletter-form" class="sticky-btn sticky-btn-primary">
        📩 メルマガ登録（無料）
      </a>
      <a
        href="https://www.amazon.co.jp/audible?tag=sengokublog20-22"
        target="_blank"
        rel="nofollow sponsored noopener"
        class="sticky-btn sticky-btn-secondary"
      >
        🎧 Audible 30日間無料
      </a>
    </div>
    <button class="sticky-close" id="sticky-close" aria-label="閉じる">✕</button>
  </div>
</div>

<script>
  // メルマガフォーム（Substack）
  const form = document.getElementById('newsletter-form') as HTMLFormElement | null;
  const thanks = document.getElementById('email-thanks') as HTMLElement | null;
  if (form && thanks) {
  }

  // スティッキーCTA
  const stickyCta = document.getElementById('sticky-cta') as HTMLElement | null;
  const stickyClose = document.getElementById('sticky-close') as HTMLElement | null;
  let ctaClosed = false;

  if (stickyCta && stickyClose) {
    window.addEventListener('scroll', () => {
      if (ctaClosed) return;
      stickyCta.classList.toggle('visible', window.scrollY > 400);
    }, { passive: true });

    stickyClose.addEventListener('click', () => {
      ctaClosed = true;
      stickyCta.classList.remove('visible');
    });
  }
</script>

<style>
  /* ── マーキー ── */
  .marquee-band {
    background: var(--gold);
    padding: 0.7rem 0;
    overflow: hidden;
  }
  .marquee-track {
    display: flex;
    width: max-content;
    animation: marquee 28s linear infinite;
  }
  @keyframes marquee {
    from { transform: translateX(0); }
    to   { transform: translateX(-50%); }
  }
  .marquee-track:hover { animation-play-state: paused; }
  .marquee-item {
    display: inline-flex;
    align-items: center;
    gap: 1rem;
    padding: 0 2rem;
    font-family: var(--font-serif);
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    color: #fff;
    white-space: nowrap;
  }
  .marquee-dot { font-size: 0.45rem; opacity: 0.7; }

  /* ── イントロバンド ── */
  .intro-band {
    background: var(--accent);
    padding: 5rem 0;
  }
  .intro-inner {
    display: flex;
    align-items: flex-start;
    gap: 3rem;
  }
  .intro-label {
    writing-mode: vertical-rl;
    font-family: var(--font-italic);
    font-size: 0.72rem;
    font-style: italic;
    letter-spacing: 0.2em;
    color: var(--gold);
    opacity: 0.7;
    flex-shrink: 0;
    margin-top: 0.2rem;
  }
  .quote-jp {
    font-family: var(--font-serif);
    font-size: clamp(1.15rem, 2.5vw, 1.7rem);
    font-weight: 700;
    color: #fff;
    line-height: 1.65;
    letter-spacing: 0.04em;
    margin-bottom: 1rem;
  }
  .quote-en {
    font-family: var(--font-italic);
    font-size: 0.95rem;
    font-style: italic;
    color: var(--gold);
    letter-spacing: 0.05em;
  }

  /* ── メルマガ ── */
  .email-section {
    background: var(--bg2);
    padding: var(--sp-xl) 0;
    border-top: 1px solid var(--rule);
    border-bottom: 1px solid var(--rule);
  }
  .email-inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 5rem;
    align-items: center;
  }
  .email-heading {
    font-family: var(--font-serif);
    font-size: clamp(1.2rem, 2.5vw, 1.7rem);
    font-weight: 700;
    color: var(--ink);
    line-height: 1.4;
    margin-bottom: 1.5rem;
    letter-spacing: 0.02em;
  }
  .email-benefits {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    margin-bottom: 1.2rem;
  }
  .email-benefits li {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    font-family: var(--font-sans);
    font-size: 0.88rem;
    color: var(--ink2);
    line-height: 1.5;
  }
  .benefit-check { color: var(--gold); font-weight: 700; flex-shrink: 0; }
  .email-note {
    font-family: var(--font-sans);
    font-size: 0.75rem;
    color: var(--ink3);
  }
  .form-label {
    display: block;
    font-family: var(--font-sans);
    font-size: 0.75rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    color: var(--ink2);
    margin-bottom: 0.6rem;
  }
  .form-row {
    display: flex;
    margin-bottom: 0.75rem;
  }
  .form-input {
    flex: 1;
    padding: 0.85rem 1.2rem;
    border: 1px solid var(--rule);
    border-right: none;
    border-radius: 2px 0 0 2px;
    font-family: var(--font-sans);
    font-size: 0.88rem;
    color: var(--ink);
    background: var(--bg);
    outline: none;
    transition: border-color 0.2s;
  }
  .form-input:focus { border-color: var(--gold); }
  .form-btn {
    padding: 0.85rem 1.6rem;
    background: var(--ink);
    color: #fff;
    font-family: var(--font-sans);
    font-size: 0.85rem;
    font-weight: 500;
    letter-spacing: 0.04em;
    border: none;
    border-radius: 0 2px 2px 0;
    cursor: pointer;
    transition: background 0.2s;
    white-space: nowrap;
  }
  .form-btn:hover { background: var(--accent); }
  .form-privacy {
    font-family: var(--font-sans);
    font-size: 0.72rem;
    color: var(--ink3);
  }
  .form-privacy a { color: var(--gold); text-decoration: underline; }
  .email-thanks {
    text-align: center;
    padding: 2.5rem;
    background: var(--bg);
    border: 1px solid var(--gold-light);
    border-radius: 4px;
  }
  .thanks-icon { font-size: 2rem; color: var(--gold); margin-bottom: 0.75rem; }
  .thanks-text {
    font-family: var(--font-serif);
    font-size: 0.95rem;
    line-height: 1.8;
    color: var(--ink2);
  }

  /* ── 記事・カテゴリ ── */
  .articles-section { background: var(--bg); }
  .categories-section { background: var(--bg2); }

  .cat-grid {
    list-style: none;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1px;
    border: 1px solid var(--rule);
    border-radius: 4px;
    overflow: hidden;
    background: var(--rule);
  }
  .cat-card {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    padding: 2rem 1.6rem;
    background: var(--bg);
    text-decoration: none;
    color: inherit;
    transition: background 0.25s;
    height: 100%;
  }
  .cat-card:hover { background: var(--ink); }
  .cat-card:hover .cat-name { color: var(--gold); }
  .cat-card:hover .cat-hook,
  .cat-card:hover .cat-link { color: #fff; }
  .cat-card:hover .cat-desc { color: rgba(255,255,255,0.5); }

  .cat-name {
    font-family: var(--font-sans);
    font-size: 0.63rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    color: var(--gold);
    text-transform: uppercase;
    transition: color 0.25s;
  }
  .cat-hook {
    font-family: var(--font-serif);
    font-size: 0.95rem;
    font-weight: 700;
    color: var(--ink);
    line-height: 1.55;
    transition: color 0.25s;
  }
  .cat-desc {
    font-family: var(--font-sans);
    font-size: 0.78rem;
    color: var(--ink3);
    line-height: 1.65;
    flex: 1;
    transition: color 0.25s;
  }
  .cat-link {
    font-family: var(--font-sans);
    font-size: 0.75rem;
    font-weight: 500;
    color: var(--gold);
    margin-top: auto;
    transition: color 0.25s;
  }

  /* ── 悩み別おすすめ ── */
  .needs-section { background: var(--bg); }
  .needs-sub {
    font-family: var(--font-sans);
    font-size: 0.78rem;
    color: var(--ink3);
    margin-top: 0.3rem;
  }
  .needs-grid {
    list-style: none;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1rem;
    margin-top: 1.6rem;
  }
  .needs-card {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    padding: 1.8rem 1.5rem;
    border: 1px solid var(--rule);
    border-radius: 4px;
    background: var(--bg);
    text-decoration: none;
    color: inherit;
    height: 100%;
    transition: background 0.22s, border-color 0.22s, transform 0.22s;
  }
  .needs-card:hover {
    background: var(--accent);
    border-color: var(--accent);
    transform: translateY(-3px);
  }
  .needs-tag {
    font-family: var(--font-sans);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    color: var(--gold);
    transition: color 0.22s;
  }
  .needs-label {
    font-family: var(--font-serif);
    font-size: 1.05rem;
    font-weight: 700;
    color: var(--ink);
    line-height: 1.4;
    transition: color 0.22s;
  }
  .needs-card:hover .needs-label { color: #fff; }
  .needs-desc {
    font-family: var(--font-sans);
    font-size: 0.78rem;
    line-height: 1.7;
    color: var(--ink3);
    flex: 1;
    transition: color 0.22s;
  }
  .needs-card:hover .needs-desc { color: rgba(255,255,255,0.55); }
  .needs-cta {
    font-family: var(--font-sans);
    font-size: 0.75rem;
    font-weight: 500;
    color: var(--gold);
    margin-top: auto;
    transition: color 0.22s;
  }
  .needs-card:hover .needs-cta { color: var(--gold-light); }

  /* ── 書籍セクション ── */
  .books-section {
    background: #2A5C43;   /* ミディアムグリーン（黒より明るめ） */
    padding: var(--sp-xl) 0;
  }
  .books-header { border-bottom-color: rgba(255,255,255,0.08); }
  .books-title { color: #fff; }
  .books-more { font-family: var(--font-sans); font-size: 0.82rem; color: var(--gold); transition: color 0.2s; }
  .books-more:hover { color: var(--gold-light); }
  .books-lead {
    font-family: var(--font-serif);
    font-size: 0.9rem;
    color: rgba(255,255,255,0.75);
    margin-bottom: 0.5rem;
    line-height: 1.8;
  }
  .books-section :global(.affiliate-disclosure) {
    color: rgba(255,255,255,0.5);
    margin-bottom: 1.5rem;
  }
  .book-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.2rem;
    margin-top: 1.5rem;
  }

  /* ── 映画コーナーバナー ── */
  .cinema-banner {
    background: #0d0f14;
    padding: 3.5rem 0;
  }
  .cinema-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2.5rem;
    flex-wrap: wrap;
  }
  .cinema-text { flex: 1; min-width: 260px; }
  .cinema-label {
    display: block;
    font-family: var(--font-sans);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    color: #e8b84b;
    margin-bottom: 0.8rem;
  }
  .cinema-heading {
    font-family: var(--font-serif);
    font-size: clamp(1.2rem, 2.5vw, 1.8rem);
    font-weight: 700;
    color: #f0eee8;
    line-height: 1.35;
    margin-bottom: 0.75rem;
    letter-spacing: 0.04em;
  }
  .cinema-body {
    font-family: var(--font-sans);
    font-size: 0.84rem;
    color: #6a6f7e;
    line-height: 1.75;
    max-width: 440px;
    margin-bottom: 1rem;
  }
  .cinema-services {
    display: flex;
    gap: 0.45rem;
  }
  .cs-badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    border-radius: 3px;
    font-family: var(--font-sans);
    font-size: 0.65rem;
    font-weight: 800;
    color: #fff;
    letter-spacing: 0;
  }
  .cinema-cta {
    display: inline-flex;
    align-items: center;
    padding: 1rem 2.2rem;
    background: #e8b84b;
    color: #0d0f14;
    font-family: var(--font-sans);
    font-size: 0.88rem;
    font-weight: 700;
    letter-spacing: 0.04em;
    border-radius: 2px;
    text-decoration: none;
    white-space: nowrap;
    flex-shrink: 0;
    transition: opacity 0.2s, transform 0.2s;
  }
  .cinema-cta:hover { opacity: 0.88; transform: translateY(-2px); }

  /* ── バナー共通 ── */
  .banner-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    padding-top: 2.5rem;
    padding-bottom: 2.5rem;
    flex-wrap: wrap;
  }
  .banner-tag {
    display: inline-block;
    font-family: var(--font-sans);
    font-size: 0.62rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    padding: 0.2em 0.8em;
    border-radius: 2px;
    text-transform: uppercase;
    margin-bottom: 0.6rem;
  }
  .banner-heading {
    font-family: var(--font-serif);
    font-size: clamp(1.05rem, 2vw, 1.35rem);
    font-weight: 700;
    line-height: 1.4;
    letter-spacing: 0.02em;
    margin-bottom: 0.4rem;
  }
  .banner-body {
    font-family: var(--font-sans);
    font-size: 0.84rem;
    line-height: 1.7;
  }
  .banner-cta {
    display: inline-flex;
    align-items: center;
    padding: 0.9rem 2rem;
    font-family: var(--font-sans);
    font-size: 0.85rem;
    font-weight: 500;
    letter-spacing: 0.04em;
    border-radius: 2px;
    text-decoration: none;
    flex-shrink: 0;
    transition: opacity 0.2s, transform 0.2s;
    white-space: nowrap;
  }
  .banner-cta:hover { opacity: 0.85; transform: translateY(-1px); }

  .audible-banner { background: var(--accent); }
  .audible-tag { background: rgba(255,255,255,0.15); color: rgba(255,255,255,0.9); }
  .audible-heading { color: #fff; }
  .audible-body { color: rgba(255,255,255,0.82); }
  .audible-cta { background: var(--gold); color: #fff; }

  .note-banner { background: var(--bg2); border-top: 1px solid var(--rule); }
  .note-tag { background: var(--gold-light); color: var(--gold-dark); }
  .note-heading { color: var(--ink); }
  .note-body { color: var(--ink2); }
  .note-cta { background: var(--ink); color: #fff; }

  /* ── スティッキーCTA ── */
  .sticky-cta {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    z-index: 200;
    transform: translateY(100%);
    transition: transform 0.35s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .sticky-cta.visible { transform: translateY(0); }
  .sticky-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: var(--ink);
    border-top: 2px solid var(--gold);
    padding: 0.9rem 1.5rem;
    gap: 1rem;
  }
  .sticky-items { display: flex; gap: 0.75rem; flex-wrap: wrap; }
  .sticky-btn {
    display: inline-flex;
    align-items: center;
    padding: 0.6rem 1.4rem;
    font-family: var(--font-sans);
    font-size: 0.82rem;
    font-weight: 500;
    letter-spacing: 0.02em;
    border-radius: 2px;
    text-decoration: none;
    transition: opacity 0.2s;
    white-space: nowrap;
  }
  .sticky-btn:hover { opacity: 0.85; }
  .sticky-btn-primary { background: var(--gold); color: #fff; }
  .sticky-btn-secondary { background: rgba(255,255,255,0.08); color: rgba(255,255,255,0.8); border: 1px solid rgba(255,255,255,0.15); }
  .sticky-close {
    background: none;
    border: none;
    color: rgba(255,255,255,0.35);
    font-size: 1rem;
    cursor: pointer;
    padding: 0.25rem;
    flex-shrink: 0;
    transition: color 0.2s;
    line-height: 1;
  }
  .sticky-close:hover { color: rgba(255,255,255,0.7); }

  /* ── レスポンシブ ── */
  @media (max-width: 1024px) {
    .cat-grid   { grid-template-columns: repeat(3, 1fr); }
    .needs-grid { grid-template-columns: repeat(2, 1fr); }
    .book-grid  { grid-template-columns: repeat(2, 1fr); }
    .email-inner { gap: 3rem; }
  }
  @media (max-width: 768px) {
    .cat-grid   { grid-template-columns: repeat(2, 1fr); }
    .email-inner { grid-template-columns: 1fr; gap: 2.4rem; }
    .book-grid { grid-template-columns: repeat(2, 1fr); }
    .banner-inner { flex-direction: column; align-items: flex-start; }
    .sticky-btn-secondary { display: none; }
  }
  @media (max-width: 480px) {
    .cat-grid { grid-template-columns: 1fr; }
    .book-grid { grid-template-columns: 1fr 1fr; gap: 0.75rem; }
    .form-row { flex-direction: column; }
    .form-input { border-right: 1px solid var(--rule); border-bottom: none; border-radius: 2px 2px 0 0; }
    .form-btn { border-radius: 0 0 2px 2px; }
    .intro-label { display: none; }
  }
</style>
