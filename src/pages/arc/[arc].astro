---
// /arc/[slug]/ → /category/[slug]/ へリダイレクト
import { THEME_ORDER } from "../../utils/arcMap";

export async function getStaticPaths() {
  return THEME_ORDER.map(arc => ({ params: { arc } }));
}

const { arc } = Astro.params;
return Astro.redirect(`/category/${arc}/`, 301);
---
